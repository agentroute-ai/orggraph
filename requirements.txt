# Dependency manifest for Streamlit Community Cloud (used as a fallback when
# uv.lock is not picked up). Installs the orggraph package, which now includes
# the dashboard dependencies (streamlit, plotly, pyvis) in its core deps.
#
# For local development use `make install` / `make dashboard` instead.
.
